version: '2'
services:
  siukibana-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/siukibana/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=siukibana
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
